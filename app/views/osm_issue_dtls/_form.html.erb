<%= form_with(model: osm_issue_dtl, local: true) do |form| %>
  <% if osm_issue_dtl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(osm_issue_dtl.errors.count, "error") %> prohibited this osm_issue_dtl from being saved:</h2>

      <ul>
        <% osm_issue_dtl.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

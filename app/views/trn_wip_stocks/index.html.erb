<p id="notice"><%= notice %></p>

<h1>Trn Wip Stocks</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @trn_wip_stocks.each do |trn_wip_stock| %>
      <tr>
        <td><%= link_to 'Show', trn_wip_stock %></td>
        <td><%= link_to 'Edit', edit_trn_wip_stock_path(trn_wip_stock) %></td>
        <td><%= link_to 'Destroy', trn_wip_stock, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Trn Wip Stock', new_trn_wip_stock_path %>
